/**
 * Midnight preset — deep blue-black.
 */

import type { PresetConfig } from './neutral.js';

export const midnight: PresetConfig = {
  name: 'midnight',
  backgroundBase: 0.05,
  surfaceStep: 0.035,
  textPrimary: 0.90,
  chromaScale: 0.85,
  warmth: -0.4,
};

export default midnight;
