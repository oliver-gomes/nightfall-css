/**
 * Warm preset — warm dark theme with brown/amber undertones.
 */

import type { PresetConfig } from './neutral.js';

export const warm: PresetConfig = {
  name: 'warm',
  backgroundBase: 0.07,
  surfaceStep: 0.04,
  textPrimary: 0.88,
  chromaScale: 0.8,
  warmth: 0.6,
};

export default warm;
